"use strict";
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _GreedyInputSelector_props;
Object.defineProperty(exports, "__esModule", { value: true });
exports.GreedyInputSelector = void 0;
const core_1 = require("@cardano-sdk/core");
const InputSelectionError_1 = require("../InputSelectionError");
const util_1 = require("../util");
const util_2 = require("./util");
class GreedyInputSelector {
    constructor(props) {
        _GreedyInputSelector_props.set(this, void 0);
        __classPrivateFieldSet(this, _GreedyInputSelector_props, props, "f");
    }
    async select(params) {
        const { preSelectedUtxo, utxo: inputs, outputs, constraints, implicitValue } = params;
        const allInputs = new Set([...inputs, ...preSelectedUtxo]);
        const utxoValues = (0, util_1.toValues)([...allInputs]);
        const outputsValues = (0, util_1.toValues)([...outputs]);
        const totalLovelaceInUtxoSet = (0, util_1.getCoinQuantity)(utxoValues);
        const totalLovelaceInOutputSet = (0, util_1.getCoinQuantity)(outputsValues);
        const totalAssetsInUtxoSet = (0, core_1.coalesceValueQuantities)(utxoValues).assets;
        const totalAssetsInOutputSet = (0, core_1.coalesceValueQuantities)(outputsValues).assets;
        const implicitCoinInput = implicitValue?.coin?.input || 0n;
        const implicitCoinOutput = implicitValue?.coin?.deposit || 0n;
        const implicitAssetInput = implicitValue?.mint || new Map();
        const totalLovelaceInput = totalLovelaceInUtxoSet + implicitCoinInput;
        const totalLovelaceOutput = totalLovelaceInOutputSet + implicitCoinOutput;
        const totalAssetsInput = (0, util_1.addTokenMaps)(totalAssetsInUtxoSet, implicitAssetInput);
        const changeLovelace = totalLovelaceInput - totalLovelaceOutput;
        const changeAssets = (0, util_1.subtractTokenMaps)(totalAssetsInput, totalAssetsInOutputSet);
        if (allInputs.size === 0 || totalLovelaceOutput > totalLovelaceInput || (0, util_1.hasNegativeAssetValue)(changeAssets))
            throw new InputSelectionError_1.InputSelectionError(InputSelectionError_1.InputSelectionFailure.UtxoBalanceInsufficient);
        const adjustedChangeOutputs = await (0, util_2.splitChangeAndComputeFee)(allInputs, outputs, changeLovelace, changeAssets, constraints, __classPrivateFieldGet(this, _GreedyInputSelector_props, "f").getChangeAddresses, 0n);
        const change = adjustedChangeOutputs.change.filter((out) => out.value.coins > 0n || (out.value.assets?.size || 0) > 0);
        if (changeLovelace - adjustedChangeOutputs.fee < 0n)
            throw new InputSelectionError_1.InputSelectionError(InputSelectionError_1.InputSelectionFailure.UtxoBalanceInsufficient);
        if (allInputs.size >
            (await constraints.computeSelectionLimit({ change, fee: adjustedChangeOutputs.fee, inputs: allInputs, outputs }))) {
            throw new InputSelectionError_1.InputSelectionError(InputSelectionError_1.InputSelectionFailure.MaximumInputCountExceeded);
        }
        return {
            remainingUTxO: new Set(),
            selection: {
                change,
                fee: adjustedChangeOutputs.fee,
                inputs: new Set([...allInputs].sort(util_1.sortUtxoByTxIn)),
                outputs
            }
        };
    }
}
exports.GreedyInputSelector = GreedyInputSelector;
_GreedyInputSelector_props = new WeakMap();
//# sourceMappingURL=GreedyInputSelector.js.map